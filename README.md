# Clustering-Analysis-using-K-Means-and-DBSCAN
This project demonstrates unsupervised learning techniques using K-Means and DBSCAN clustering algorithms. It explores how different algorithms perform on structured (blob) and non-linear (moon-shaped) datasets. The project includes cluster visualization, optimal cluster selection using the Elbow Method, and a comparative analysis
